import { useEffect } from 'react';
import { useScrollReveal } from '../hooks/useGlobalEffects.js';

const ABOUT_HTML = '      <div class="dot-grid-fade"></div>\n\n      <!-- HERO -->\n      <div class="about-me-hero">\n        <div class="about-me-hero-bar"></div>\n        <div class="about-me-left">\n          <h1 class="about-me-name">Sujan Kamble</h1>\n          <p class="about-me-bio">Hi, I’m a <strong>UX designer with 13+ years of experience</strong> based in Pune, doing something I love, turning complex systems into clear, human experiences. I’ve spent over a decade in high-stakes worlds like enterprise finance, banking, places where a confusing button doesn\'t just cause a sigh, it causes an operational crisis.</p>\n          <p class="about-me-bio">I thrive in the messy grey areas of design, bringing rigour to research and deep empathy to the people using the final product. At the end of the day, my philosophy is simple: <strong>figure out what a human actually needs to get done, and get rid of the roadblocks, whatever is blocking their way.</strong></p>\n          <div class="about-me-avail">\n            <div class="about-me-avail-dot"></div>\n            <span>Available for full-time roles &nbsp;·&nbsp; Open to remote &nbsp;·&nbsp; Pune, India</span>\n          </div>\n        </div>\n        <div class="about-me-right">\n          <div class="about-me-photo">\n            <img src="/Portfolio-2026/images/Profile picture 2.jpeg" alt="Sujan Kamble" />\n          </div>\n        </div>\n      </div>\n\n      <!-- STATS -->\n      <div class="about-me-stats">\n        <div class="about-me-stat">\n          <div class="about-me-stat-num">13<span>+</span></div>\n          <div class="about-me-stat-label">Years in UX design</div>\n        </div>\n        <div class="about-me-stat">\n          <div class="about-me-stat-num">5</div>\n          <div class="about-me-stat-label">Case studies across 4 engagements</div>\n        </div>\n        <div class="about-me-stat">\n          <div class="about-me-stat-num">6</div>\n          <div class="about-me-stat-label">Industries designed for</div>\n        </div>\n        <div class="about-me-stat">\n          <div class="about-me-stat-num">8</div>\n          <div class="about-me-stat-label">Max team size collaborated with</div>\n        </div>\n      </div>\n\n      <!-- SKILLS -->\n      <div class="about-me-section">\n        <p class="about-me-section-label">What I do</p>\n        <div class="skills-grid">\n          <div class="skill-card">\n            <div class="skill-card-icon">\n              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FF6B35" stroke-width="1.5"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>\n            </div>\n            <h3 class="skill-card-title">Research & Strategy</h3>\n            <p class="skill-card-desc">Stakeholder interviews, heuristic evaluations, competitor analysis, and user journey mapping. Turning ambiguous briefs into directions grounded in evidence not assumption.</p>\n          </div>\n          <div class="skill-card">\n            <div class="skill-card-icon">\n              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FF6B35" stroke-width="1.5"><rect x="5" y="2" width="14" height="20" rx="2"/><line x1="9" y1="7" x2="15" y2="7"/><line x1="9" y1="11" x2="15" y2="11"/><line x1="9" y1="15" x2="12" y2="15"/></svg>\n            </div>\n            <h3 class="skill-card-title">Product & App Design</h3>\n            <p class="skill-card-desc">End-to-end product design across web and mobile from information architecture and wireframes through to high-fidelity prototypes and annotated developer handoff.</p>\n          </div>\n          <div class="skill-card">\n            <div class="skill-card-icon">\n              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FF6B35" stroke-width="1.5"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>\n            </div>\n            <h3 class="skill-card-title">Design Systems & Handoff</h3>\n            <p class="skill-card-desc">Building and evolving design systems that scale. UI guidelines, component libraries, accessibility standards, and annotation frameworks that outlast any individual designer\'s involvement.</p>\n          </div>\n        </div>\n      </div>\n\n      <!-- TOOLS -->\n      <div class="about-me-section">\n        <p class="about-me-section-label">Tools & Methods</p>\n        <div class="tools-row">\n          <span class="tool-pill">Figma</span>\n          <span class="tool-pill">FigJam</span>\n          <span class="tool-pill">Miro</span>\n          <span class="tool-pill">Sketch</span>\n          <span class="tool-pill">Zeplin</span>\n          <span class="tool-pill">Jira</span>\n          <span class="tool-pill">Confluence</span>\n          <span class="tool-pill">InVision</span>\n        </div>\n      </div>\n\n      <!-- INDUSTRIES -->\n      <div class="about-me-section">\n        <p class="about-me-section-label">Industries</p>\n        <div class="industries-row">\n          <span class="industry-pill">Finance &amp; Audit</span>\n          <span class="industry-pill">Retail Banking</span>\n          <span class="industry-pill">Pharmaceutical Manufacturing</span>\n          <span class="industry-pill">Insurance</span>\n          <span class="industry-pill">Healthcare</span>\n          <span class="industry-pill">Retail</span>\n        </div>\n      </div>\n\n      </div>\n\n\n      \n\n    </div>\n\n  </div><!-- /case-pages-container -->\n\n  <script>\n    // ── CURSOR\n    const cursor=document.getElementById("cursor"),ring=document.getElementById("cursorRing");\n    let mx=0,my=0,rx=0,ry=0;\n    document.addEventListener("mousemove",e=>{mx=e.clientX;my=e.clientY;cursor.style.transform=`translate(${mx-4}px,${my-4}px)`;});\n    (function tick(){rx+=(mx-rx-15)*.1;ry+=(my-ry-15)*.1;ring.style.transform=`translate(${rx}px,${ry}px)`;requestAnimationFrame(tick);})();\n    document.querySelectorAll("a,button,.card,.card-flare-wrap,.filter-btn").forEach(el=>{\n      el.addEventListener("mouseenter",()=>ring.classList.add("hovering"));\n      el.addEventListener("mouseleave",()=>ring.classList.remove("hovering"));\n    });\n\n    // ── PARTICLE CANVAS\n    const canvas=document.getElementById("heroCanvas"),ctx=canvas.getContext("2d");\n    let W,H,particles=[];\n    function resize(){W=canvas.width=canvas.offsetWidth;H=canvas.height=canvas.offsetHeight;}\n    resize();window.addEventListener("resize",()=>{resize();initP();});\n    function initP(){particles=[];const count=Math.floor(W*H/9000);for(let i=0;i<count;i++)particles.push({x:Math.random()*W,y:Math.random()*H,r:document.body.classList.contains(\'light-mode\')?Math.random()*2.4+.8:Math.random()*2.0+.4,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25,alpha:document.body.classList.contains(\'light-mode\')?Math.random()*.5+.45:Math.random()*.55+.25,color:document.body.classList.contains(\'light-mode\')?"rgba(42,109,255,":(Math.random()>.55?"rgba(255,120,55,":Math.random()>.4?"rgba(190,70,240,":Math.random()>.5?"rgba(100,210,255,":"rgba(240,240,255,")});}\n    initP();\n    let mouseX=W/2,mouseY=H/2;\n    canvas.addEventListener("mousemove",e=>{const r=canvas.getBoundingClientRect();mouseX=e.clientX-r.left;mouseY=e.clientY-r.top;});\n    function drawP(){ctx.clearRect(0,0,W,H);particles.forEach(p=>{const dx=p.x-mouseX,dy=p.y-mouseY,dist=Math.sqrt(dx*dx+dy*dy);if(dist<100){p.x+=dx/dist*.4;p.y+=dy/dist*.4;}p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=W;if(p.x>W)p.x=0;if(p.y<0)p.y=H;if(p.y>H)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=p.color+p.alpha+")";ctx.fill();});for(let i=0;i<particles.length;i++)for(let j=i+1;j<particles.length;j++){const dx=particles[i].x-particles[j].x,dy=particles[i].y-particles[j].y,d=Math.sqrt(dx*dx+dy*dy);if(d<80){ctx.beginPath();ctx.moveTo(particles[i].x,particles[i].y);ctx.lineTo(particles[j].x,particles[j].y);ctx.strokeStyle=`rgba(255,107,53,${.06*(1-d/80)})`;ctx.lineWidth=.5;ctx.stroke();}}requestAnimationFrame(drawP);}\n    drawP();\n\n    // ── NAV\n    window.addEventListener("scroll",()=>{\n      const nav=document.getElementById("nav");\n      nav.classList.toggle("scrolled", window.scrollY > 80);\n    }, {passive:true});\n\n    // ── REVEAL\n    // ── REVEAL + STAGGER OBSERVER ──\n    const obs = new IntersectionObserver(entries=>{\n      entries.forEach(e=>{\n        if(e.isIntersecting){\n          e.target.classList.add("visible");\n          obs.unobserve(e.target);\n        }\n      });\n    },{threshold:0.06, rootMargin:"0px 0px -40px 0px"});\n\n    const staggerObs = new IntersectionObserver(entries=>{\n      entries.forEach(e=>{\n        if(e.isIntersecting){\n          // Fire stagger-child reveals for children of this container\n          e.target.querySelectorAll(".stagger-child").forEach(child=>{\n            child.classList.add("visible");\n          });\n          staggerObs.unobserve(e.target);\n        }\n      });\n    },{threshold:0.08, rootMargin:"0px 0px -30px 0px"});\n\n    function observeReveals(){\n      document.querySelectorAll(".reveal,.reveal-left,.reveal-scale").forEach(r=>{\n        r.classList.remove("visible");\n        obs.observe(r);\n      });\n      document.querySelectorAll(".stagger-wrap").forEach(w=>{\n        w.querySelectorAll(".stagger-child").forEach(c=>c.classList.remove("visible"));\n        staggerObs.observe(w);\n      });\n    }\n    observeReveals();\n\n    // ── PARALLAX ──\n    function initParallax(){\n      const glows = document.querySelectorAll(".cs-hero-glow, .hero-glow");\n      window.addEventListener("scroll", ()=>{\n        const sy = window.scrollY;\n        glows.forEach(g=>{\n          if(g.closest(".case-page") && g.closest(".case-page").style.display === "none") return;\n          g.style.transform = `translateY(${sy * 0.18}px)`;\n        });\n      }, {passive:true});\n    }\n    initParallax();\n\n    // ── FILTER\n    document.querySelectorAll(".filter-btn").forEach(btn=>{\n      btn.addEventListener("click",()=>{\n        document.querySelectorAll(".filter-btn").forEach(b=>b.classList.remove("active"));\n        btn.classList.add("active");\n        const f = btn.dataset.filter;\n        document.querySelectorAll("[data-category]").forEach(c=>{\n          const match = f === "all" || c.dataset.category === f;\n          if(match){\n            c.style.display = "";\n            c.classList.add("visible");\n            requestAnimationFrame(()=>{\n              c.style.transition = "opacity .35s ease, transform .35s ease";\n              c.style.opacity = "1";\n              c.style.transform = "translateY(0)";\n            });\n          } else {\n            c.style.transition = "opacity .25s ease, transform .25s ease";\n            c.style.opacity = "0";\n            c.style.transform = "translateY(8px)";\n            setTimeout(()=>{ c.style.display = "none"; }, 260);\n          }\n        });\n      });\n    });\n\n    // ── PAGE MANAGER\n    function showPage(id){\n      if(typeof closeDrawer === \'function\') closeDrawer();\n      const nav = document.getElementById("nav");\n      const isLight = document.body.classList.contains("light-mode");\n\n      // Hide all case pages first\n      document.querySelectorAll(".case-page").forEach(p => p.style.display = "none");\n\n      if(id === "home"){\n        document.body.className = "show-home" + (isLight ? " light-mode" : "");\n        nav.classList.remove("solid");\n        window.scrollTo({top:0, behavior:"instant"});\n        setTimeout(observeReveals, 50);\n      } else {\n        const targetId = id === "about" ? "case-about" : id;\n        const target = document.getElementById(targetId);\n        if(target){\n          document.body.className = "show-case" + (isLight ? " light-mode" : "");\n          target.style.display = "block";\n          nav.classList.add("solid");\n          window.scrollTo({top:0, behavior:"instant"});\n        }\n      }\n    }\n\n    function scrollToWork(){\n      setTimeout(()=>{const w=document.getElementById("work");if(w)w.scrollIntoView({behavior:"smooth"});},50);\n    }\n\n    // ── CASE STUDY NAVIGATION\n    // Page state managed via body class initialized in HTML\n    function showCase(id){\n      showPage("case-"+id);\n      setTimeout(observeReveals, 50);\n    }\n\n\n    // Direct case study navigation\n    document.addEventListener("click", e => {\n      const card = e.target.closest("[data-case]");\n      if(card && card.dataset.case) showCase(card.dataset.case);\n    });\n\n    document.querySelectorAll(".cs-pag-btn").forEach(btn=>{\n      btn.addEventListener("click",()=>{window.scrollTo({top:0,behavior:"instant"});});\n    });\n\n    // smooth scroll for sidenav links\n\n    // ── FLARE EFFECT\n    (function(){\n      const wraps = document.querySelectorAll(\'.card-flare-wrap\');\n      wraps.forEach(wrap => {\n        const canvas = wrap.querySelector(\'.card-flare-canvas\');\n        if(!canvas) return;\n        const ctx = canvas.getContext(\'2d\');\n        let angle = 0;\n        let raf = null;\n        const color = getComputedStyle(wrap).getPropertyValue(\'--card-color\').trim() || \'#FF6B35\';\n\n        function resize(){\n          canvas.width = wrap.offsetWidth + 4;\n          canvas.height = wrap.offsetHeight + 4;\n        }\n        resize();\n\n        function draw(){\n          const w = canvas.width, h = canvas.height;\n          ctx.clearRect(0,0,w,h);\n\n          // Rotating conic gradient via arc sweep\n          const cx = w/2, cy = h/2;\n          const perimX = cx + cx * Math.cos(angle);\n          const perimY = cy + cy * Math.sin(angle);\n\n          // Draw glowing border sweep\n          const r = Math.max(w,h);\n          const grad = ctx.createConicalGradient\n            ? ctx.createConicalGradient(cx, cy, angle)\n            : null;\n\n          // Fallback: draw a moving bright spot along the border path\n          const borderPath = new Path2D();\n          const rad = 14; // border-radius\n          borderPath.roundRect(2, 2, w-4, h-4, rad);\n\n          // Draw the glow border\n          ctx.save();\n          ctx.strokeStyle = \'transparent\';\n          ctx.lineWidth = 2;\n          ctx.stroke(borderPath);\n\n          // Animated highlight position along perimeter\n          const perimeter = 2*(w+h) - 8*rad + 2*Math.PI*rad;\n          const spotPos = ((angle/(Math.PI*2)) % 1) * perimeter;\n\n          // Draw glow spots\n          for(let i = 0; i < 3; i++){\n            const pos = (spotPos + i * perimeter/3) % perimeter;\n            const {x, y} = getPosOnRect(pos, 2, 2, w-4, h-4, rad);\n\n            const spotGrad = ctx.createRadialGradient(x, y, 0, x, y, 60);\n            const alpha = i === 0 ? 0.9 : 0.4;\n            spotGrad.addColorStop(0, hexToRgba(color, alpha));\n            spotGrad.addColorStop(0.3, hexToRgba(color, alpha*0.5));\n            spotGrad.addColorStop(1, \'transparent\');\n\n            ctx.fillStyle = spotGrad;\n            ctx.fillRect(0, 0, w, h);\n          }\n\n          // Clip to border ring only\n          ctx.globalCompositeOperation = \'destination-in\';\n          ctx.lineWidth = 3;\n          ctx.strokeStyle = \'white\';\n          ctx.stroke(borderPath);\n          ctx.globalCompositeOperation = \'source-over\';\n          ctx.restore();\n\n          angle += 0.025;\n          raf = requestAnimationFrame(draw);\n        }\n\n        function getPosOnRect(pos, x1, y1, x2, y2, r){\n          const w = x2-x1, h = y2-y1;\n          const top = w, right = w+h, bottom = w+h+w, left = w+h+w+h;\n          if(pos < top) return {x: x1+pos, y: y1};\n          if(pos < right) return {x: x2, y: y1+(pos-top)};\n          if(pos < bottom) return {x: x2-(pos-right), y: y2};\n          return {x: x1, y: y2-(pos-bottom)};\n        }\n\n        function hexToRgba(hex, alpha){\n          hex = hex.replace(\'#\',\'\');\n          if(hex.length===3) hex=hex.split(\'\').map(c=>c+c).join(\'\');\n          const r=parseInt(hex.substr(0,2),16);\n          const g=parseInt(hex.substr(2,2),16);\n          const b=parseInt(hex.substr(4,2),16);\n          return `rgba(${r},${g},${b},${alpha})`;\n        }\n\n        wrap.addEventListener(\'mouseenter\', ()=>{ resize(); draw(); });\n        wrap.addEventListener(\'mouseleave\', ()=>{ if(raf){cancelAnimationFrame(raf);raf=null;} ctx.clearRect(0,0,canvas.width,canvas.height); });\n        window.addEventListener(\'resize\', resize);\n      });\n    })();\n\n\n\n\n\n\n\n\n    // ── FLIPBOARD TYPEWRITER ──\n    (function(){\n      const container = document.getElementById(\'wordShuffle\');\n      if(!container) return;\n\n      const words = [\'experiences\', \'clarity\', \'systems\', \'decisions\'];\n      let idx = 0, busy = false;\n\n      const STAGGER_OUT = 28, STAGGER_IN = 52;\n      const OUT_DUR = 180, IN_DUR = 240;\n      const GAP = 60, HOLD = 3000;\n\n      function makeChar(ch){\n        const wrap = document.createElement(\'span\');\n        wrap.className = \'flip-char\';\n        const inner = document.createElement(\'span\');\n        inner.className = \'flip-char-inner\';\n        inner.textContent = ch === \' \' ? \'\xa0\' : ch;\n        wrap.appendChild(inner);\n        return { wrap, inner };\n      }\n\n      function buildWord(word, animateIn){\n        container.innerHTML = \'\';\n        word.split(\'\').forEach((ch, i) => {\n          const { wrap, inner } = makeChar(ch);\n          container.appendChild(wrap);\n          if(animateIn){\n            inner.style.opacity = \'0\';\n            inner.style.transform = \'rotateX(-90deg) scaleY(.8)\';\n            setTimeout(() => {\n              inner.style.animation = `flipIn ${IN_DUR}ms cubic-bezier(.16,1,.3,1) forwards`;\n            }, i * STAGGER_IN);\n          }\n        });\n      }\n\n      function flipTo(newWord){\n        if(busy) return;\n        busy = true;\n        const inners = Array.from(container.querySelectorAll(\'.flip-char-inner\'));\n        inners.forEach((inner, i) => {\n          setTimeout(() => {\n            inner.style.animation = \'none\';\n            void inner.offsetWidth;\n            inner.style.animation = `flipOut ${OUT_DUR}ms cubic-bezier(.4,0,1,1) forwards`;\n          }, i * STAGGER_OUT);\n        });\n        const outDone = inners.length * STAGGER_OUT + OUT_DUR + GAP;\n        setTimeout(() => {\n          buildWord(newWord, true);\n          const inDone = newWord.length * STAGGER_IN + IN_DUR;\n          setTimeout(() => { busy = false; }, inDone);\n        }, outDone);\n      }\n\n      buildWord(words[0], true);\n      setInterval(() => {\n        idx = (idx + 1) % words.length;\n        flipTo(words[idx]);\n      }, HOLD);\n    })();\n\n\n    // ── THEME TOGGLE ──\n    (function(){\n      const btn = document.getElementById(\'themeToggle\');\n      if(!btn) return;\n      // Theme already restored on init\n      btn.addEventListener(\'click\', () => {\n        const isLight = document.body.classList.toggle(\'light-mode\');\n        localStorage.setItem(\'theme\', isLight ? \'light\' : \'dark\');\n        // Re-init particles so colours match new theme\n        if(typeof initP === \'function\') initP();\n      });\n    })();\n\n\n    // ── HAMBURGER MENU ──\n    (function(){\n      const hamburger = document.getElementById(\'navHamburger\');\n      const drawer = document.getElementById(\'navDrawer\');\n      if(!hamburger || !drawer) return;\n\n      window.closeDrawer = function(){\n        hamburger.classList.remove(\'open\');\n        drawer.classList.remove(\'open\');\n      };\n\n      hamburger.addEventListener(\'click\', () => {\n        const isOpen = drawer.classList.toggle(\'open\');\n        hamburger.classList.toggle(\'open\', isOpen);\n      });\n\n      // Close drawer when clicking outside\n      document.addEventListener(\'click\', e => {\n        if(!hamburger.contains(e.target) && !drawer.contains(e.target)){\n          closeDrawer();\n        }\n      });\n\n      // Close drawer on resize back to desktop\n      window.addEventListener(\'resize\', () => {\n        if(window.innerWidth > 768) closeDrawer();\n      });\n\n      // Mobile toggle removed from drawer — themeToggle in nav-controls handles all viewports\n    })();\n\n    // ── CS PROJECT SUMMARY: Count-up + 6° Tilt + Custom Cursor ──\n    (function(){\n\n      // ── Custom cursor pill ──\n      // Only wire up on devices that support hover (not touch)\n      if (window.matchMedia(\'(hover: hover)\').matches) {\n        document.querySelectorAll(\'.cs-project-summary-wrap\').forEach(wrap => {\n          const cursor = wrap.querySelector(\'.cps-cursor\');\n          if (!cursor) return;\n\n          wrap.addEventListener(\'mouseenter\', () => {\n            cursor.classList.add(\'cps-cursor-visible\');\n          });\n          wrap.addEventListener(\'mouseleave\', () => {\n            cursor.classList.remove(\'cps-cursor-visible\');\n          });\n          wrap.addEventListener(\'mousemove\', e => {\n            cursor.style.left = e.clientX + \'px\';\n            cursor.style.top  = e.clientY + \'px\';\n          });\n        });\n      }\n\n      // ── Count-up animation ──\n      function parseVal(str) {\n        // Extract leading number, preserve suffix (e.g. "12+" → {n:12, suffix:"+"}, "2 Years" → {n:2, suffix:" Years"})\n        const m = str.match(/^(\\d+(?:\\.\\d+)?)(.*)/);\n        if (!m) return null;\n        return { n: parseFloat(m[1]), suffix: m[2] };\n      }\n\n      function countUp(el, from, to, suffix, duration) {\n        const start = performance.now();\n        function step(now) {\n          const p = Math.min((now - start) / duration, 1);\n          // ease out cubic\n          const ease = 1 - Math.pow(1 - p, 3);\n          const cur = Math.round(from + (to - from) * ease);\n          el.textContent = cur + suffix;\n          if (p < 1) requestAnimationFrame(step);\n          else el.textContent = to + suffix;\n        }\n        requestAnimationFrame(step);\n      }\n\n      const countObs = new IntersectionObserver((entries) => {\n        entries.forEach(entry => {\n          if (!entry.isIntersecting) return;\n          const card = entry.target;\n          card.querySelectorAll(\'.cps-outcome-val\').forEach(el => {\n            const parsed = parseVal(el.textContent.trim());\n            if (!parsed || parsed.n === 0) return;\n            const original = el.textContent.trim();\n            el.dataset.original = original;\n            countUp(el, 0, parsed.n, parsed.suffix, 1200);\n          });\n          countObs.unobserve(card);\n        });\n      }, { threshold: 0.3 });\n\n      document.querySelectorAll(\'.cs-project-summary\').forEach(card => {\n        countObs.observe(card);\n      });\n\n      // ── 6° Tilt on hover ──\n      document.querySelectorAll(\'.cs-project-summary-wrap\').forEach(wrap => {\n        const card = wrap.querySelector(\'.cs-project-summary\');\n        if (!card) return;\n\n        wrap.addEventListener(\'mousemove\', e => {\n          const r = card.getBoundingClientRect();\n          const x = (e.clientX - r.left) / r.width  - 0.5;\n          const y = (e.clientY - r.top)  / r.height - 0.5;\n          card.style.transform = `rotateY(${x * 12}deg) rotateX(${-y * 12}deg)`;\n          card.style.boxShadow = `${-x * 12}px ${y * 12}px 40px rgba(0,0,0,.18)`;\n        });\n\n        wrap.addEventListener(\'mouseleave\', () => {\n          card.style.transform = \'rotateY(0deg) rotateX(0deg)\';\n          card.style.boxShadow = \'none\';\n        });\n      });\n\n    }());\n';

export default function AboutPage() {
  useScrollReveal();

  useEffect(() => {
    window.scrollTo({ top: 0, behavior: 'instant' });
  }, []);

  return (
    <div className="case-page about-me-page" id="case-about">
      <div dangerouslySetInnerHTML={{ __html: ABOUT_HTML }} />
    </div>
  );
}
